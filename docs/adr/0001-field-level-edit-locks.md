# Field-level edit locks for shared Workspace editing

Opportunity Atlas will let all team members edit its single shared Workspace, using temporary field-level edit locks to prevent competing writes to the same field. This preserves useful parallel collaboration while avoiding the scope and complexity of live text merging and full conflict resolution.
